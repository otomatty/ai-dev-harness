## Claude-only: sensor `aidlc-pytest`

Runs the project's pytest suite + coverage floor; fires on Python writes and surfaces failing tests / coverage gaps as SENSOR_FAILED at the stage gate.

Available in the Claude distribution only.
