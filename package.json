{
  "name": "ai-dev-harness",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "check": "bun run scripts/check.ts",
    "test": "bun test"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
