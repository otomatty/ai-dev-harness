{
  "name": "ai-dev-harness",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "check": "bun run scripts/check.ts",
    "test": "bun test"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  }
}
